{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "ShortenerService/Dockerfile"
  },
  "deploy": {
    "startCommand": "dotnet ShortenerService.dll",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 300
  }
}
